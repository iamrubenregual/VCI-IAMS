@{
    ViewData["Title"] = "Access Denied";
}

<div class="alert alert-warning mt-4" role="alert">
    <h4 class="alert-heading"><i class="bi bi-shield-exclamation"></i> Access denied</h4>
    <p>Your account does not have permission to perform this action.</p>
    <a asp-controller="Home" asp-action="Index" class="btn btn-outline-secondary">Return to dashboard</a>
</div>